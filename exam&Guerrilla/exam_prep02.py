def print_numbers(n, k):
	def inner(n, s):
		if n == 0:
			if
		else:


	inner(n, 0)

# define fn about reverse digit
def reverse(n, s):
	if n == 0:
		return 0;
	else:
		return reverse();

# num % 10 * 10**s + num / 10 (s=4)
# 13597 -> num % 10 * 10* 10**s + num /10 (s=1) -> 97531